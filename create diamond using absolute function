//another way to create diamond pattern using absolute function
function diamond(a){
  for(var i=-a; i<=a; i++){
    var pattern= "";
    for(var j=-a; j<=a; j++){
      if(Math.abs(i)+Math.abs(j)<=a){
       pattern +="* "; 
      }
      else{
        pattern += "  ";
      }
    }
    console.log(pattern);
  }
}

diamond(5);

if we change the condition of if statement it gives different diamond shape
for ex
if(Math.abs(i)+Math.abs(j)==a) will give outline of diamond shape
if(Math.abs(i)+Math.abs(j)>=a) will give daimond with shape empty and outside fill with *
if(Math.abs(i)==Math.abs(j)) will give cross shape of *
if(Math.abs(i)>=Math.abs(j)) will give big cross shape check example.
if(Math.abs(i)<=Math.abs(j)) will give big cross shape check example this is different then above one.

